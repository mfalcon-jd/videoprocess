Deploy Instructions

Run the following command

pip install -r requirements.txt

Create a Media Folder on the root direcitory and put the  Video File inside the media folder

Create a file called local_settings.py and inside put the following:
postgresql = {'pguser': 'user',
              'pgpasswd': 'password',
              'pghost': 'host',
              'pgport': port,
              'pgdb': 'database'
             }

To run execute
python main.py
